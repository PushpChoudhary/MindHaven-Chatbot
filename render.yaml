services:
  - type: web
    name: mindhaven-chatbot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn chatbot:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"